{
  "name": "ui-tester-chatbot",
  "version": "1.0.0",
  "description": "Node.js chatbot that uses Ollama and Playwright MCP to run UI tests",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "check": "node --check src/index.js && node --check src/agent.js && node --check src/prompts.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@playwright/mcp": "^0.0.70"
  }
}
